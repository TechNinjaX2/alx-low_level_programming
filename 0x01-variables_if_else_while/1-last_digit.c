#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 *
 * main - Entry Point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int n;

	srand(time(0));
	n = rand() - RAND_MAX / 2;
	z = ((n%10))
	for (;n>5;)
		printf("Last digit of %d", n "is %d",z "and is greater than 5")
	return (0);
}
