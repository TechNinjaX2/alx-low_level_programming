#include <stdio.h>

/**
 * main - prints name of file
 * @argc: number of elments
 * @argv: the array
 * Return: 0
 */
int main(int argc, char *argv[])
{
	int i = 0;

	(void)argc;
	printf("%s\n", argv[i]);

	return (0);
}
