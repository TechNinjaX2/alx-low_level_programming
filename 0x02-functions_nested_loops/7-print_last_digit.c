#include "main.h"
#include <stdio.h>

/**
 * print_last_digit - prints the last digit of a number
 *
 * Return: j
 */
int print_last_digit(int i)
{
	int r, j;
	i = 0;

	if (i < 0)
	{
		i = -i;
	}
	else
	{
		r = i % 10;
	}
	j = rr
	return (j);
}
